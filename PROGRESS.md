# 进度追踪

## 🎯 当前目标
修复坦克 AI 开局 5 秒内路径为空后原地发呆的问题。

## ✅ 完成进度
- [x] 插桩 AI `canMove`、逻辑碰撞、视觉墙体接触和静止帧
- [x] 跑 60 局粗检：发现视觉贴墙频繁但需区分真实碰撞盒
- [x] 跑 100 局真实碰撞盒检测：无出生嵌墙、无逻辑嵌墙
- [x] 发现少量路径为空后原地不动超过 1 秒的情况
- [x] 给 AI 寻路失败增加短距离备用移动目标
- [x] 复跑 100 局前 5 秒专项检测通过

## 📋 关键决策
- 按 Ponytail 原则不引入新依赖，不新增抽象层。
- 通关后的重新开始入口只保留 Header 中的奖杯/刷新按钮。
- 困难模式不再走随机贪心挖洞，避免为追求 60 空格导致生成过慢或偶发失败。
- 红色冲突优先：行/列/宫必须填满 1–9 且没有冲突才标绿。
- 坦克“撞墙”按真实 `canMove` 碰撞盒判断；视觉矩形贴墙不等同于逻辑撞墙。
- 备用移动不再二次依赖 BFS；直接设置一个短距离可走目标，避免路径为空循环。

## 🔄 上下文恢复信息
坦克 AI 路径为空发呆已修。验证：100 局开局 5 秒中出生嵌墙 0、逻辑嵌墙 0、连续撞墙阻挡超阈值 0、静止超过 1 秒 0；基础开局/射击/结束冒烟通过。
